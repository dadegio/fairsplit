services:
  fairsplit:
    image: node:20
    working_dir: /app
    volumes: [".:/app"]
    ports: ["4000:4000", "5173:5173"]
    command: sh -lc "npm install && npm run db:push && npm run db:seed && npm run dev"
    environment:
      DATABASE_URL: file:./dev.db
      PORT: 4000
